# Эталонный дамп реальной доски Jira (содержит имена и тикеты) — не публикуем
part.html

# Сырые дампы ответов API с реальными данными — кладём сюда, в гит не идут.
# В репозиторий коммитим только обезличенные *.sample.json (см. tools/anonymize.mjs).
fixtures/*.raw.json
fixtures/raw/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Локальные правила работы (не публикуем)
.rules/

# Редакторы / IDE
.vscode/
.idea/
*.swp
*~

# Зависимости / сборка (на будущее)
node_modules/
dist/
build/
*.zip
*.log
